pyudev>=0.24.0
psutil>=5.9.0
watchdog>=3.0.0
python-daemon>=2.3.0
pyyaml>=6.0
rich>=13.0.0
matplotlib>=3.7.0
# Optional fuzzy-hash backends. Installer will try these best-effort.
# Keep them out of hard requirements because native builds can fail
# on some Python/distros (for example Python 3.13 without matching wheels).
